# AKUMA's Advanced Scanner Configuration for Quick Testing
# Testing only with ms17-010 module on a small subset

# Subnets to scan (small range for testing)
SUBNETS=(
    "192.168.112.20/30"
)

# Very conservative settings for stability
MAX_PARALLEL=2
TIMEOUT_PER_HOST=60
NMAP_THREADS=10

# Authentication settings
AUTHENTICATED_SCAN=false
USERNAME="pentest"
PASSWORD="43CdfGjkCgbCjw"
DOMAIN=""

# Debug mode - ON
DEBUG_MODE=true
