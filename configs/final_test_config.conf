# AKUMA's Advanced Scanner - FINAL TEST CONFIGURATION
# Complete test of 192.168.112.0/22 subnet (1024 IPs)

# Target subnet for final testing
SUBNETS=(
    "192.168.112.0/22"
)

# Optimized settings for full subnet scan
MAX_PARALLEL=15
TIMEOUT_PER_HOST=180
NMAP_THREADS=100

# Enable all reliability features
RESUME_SCAN=true
CHECKPOINT_INTERVAL=50
AUTO_RETRY_FAILED=true
MAX_RETRY_ATTEMPTS=2

# Authentication settings
AUTHENTICATED_SCAN=false
USERNAME="pentest"
PASSWORD="43Cфывфыв"
DOMAIN=""

# Full debug logging for testing
DEBUG_MODE=true

# Performance optimization
export NXCDB_TIMEOUT=30
export NXCDB_MAX_CONNECTIONS=10
