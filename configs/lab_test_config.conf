# AKUMA's Scanner - LAB NETWORK TEST
# Testing lab 10.0.13.0/24 network

SUBNETS=(
    "10.0.13.0/24"
)

# Fast settings for lab test
MAX_PARALLEL=10
TIMEOUT_PER_HOST=60
NMAP_THREADS=50

# Authentication (testing different creds)
AUTHENTICATED_SCAN=true
USERNAME="admin"
PASSWORD="password"
DOMAIN=""

DEBUG_MODE=true
