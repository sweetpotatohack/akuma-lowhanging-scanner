# AKUMA's Advanced Scanner Configuration for Single Subnet Testing
# Testing only 192.168.112.0/24 subnet (256 IPs)

# Subnets to scan (space-separated)
SUBNETS=(
    "192.168.112.0/24"
)

# Parallel execution settings - более консервативные для стабильности
MAX_PARALLEL=10
TIMEOUT_PER_HOST=180
NMAP_THREADS=50

# Authentication settings
AUTHENTICATED_SCAN=false
USERNAME="pentest"
PASSWORD="вфывфыв"
DOMAIN=""

# Debug mode - включаем для подробного логирования
DEBUG_MODE=true

# Дополнительные настройки для стабильности
export NXCDB_TIMEOUT=30
export NXCDB_MAX_CONNECTIONS=5
